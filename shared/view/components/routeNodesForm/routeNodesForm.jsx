import React from 'react';

import './routeNodesForm.scss';

const RouteNodesForm = (props) => {
  const { } = props;

  return (
    <form className="route-nodes-form">
      mockTEXT
    </form>
  );
};

export default RouteNodesForm;
